services:
  catalog:
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: "0.2"
          memory: 200M

  order:
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: "0.2"
          memory: 200M

  payment:
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: "0.15"
          memory: 200M

  notification:
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: "0.1"
          memory: 150M
